import React from 'react';
import ScheduleServicesPresentation from '../presentation';

const ScheduleServicesContainer = props => {
  return <ScheduleServicesPresentation {...props} />;
};

export default ScheduleServicesContainer;
