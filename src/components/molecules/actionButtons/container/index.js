import React from 'react';
import ActionButtonsPresentation from '../presentation';

const ActionButtonsContainer = props => {
  return <ActionButtonsPresentation {...props} />;
};

export default ActionButtonsContainer;
