import React from 'react';
import ScheduleAvailablePagePresentation from '../presentation';

const ScheduleAvailablePageContainer = props => {
  return <ScheduleAvailablePagePresentation {...props} />;
};

export default ScheduleAvailablePageContainer;
